import {StyleSheet} from 'react-native'

export const styles_logo = StyleSheet.create({
    imgView: {
        width: '100%',
        height: 200,
        borderRadius: 100,
        marginTop: 20,
        marginBottom: 5,
        alignItems: 'center',
    },
    img: {
        width: 200,
        height: 200,
        borderRadius: 100,
        overflow: 'hidden',
        resizeMode: 'cover',
    },
})
